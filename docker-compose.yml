services:
  postgres:
    image: postgres:18
    
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=jetstream
    
    ports:
      - "5432:5432"

    volumes:
      - ./db_volume:/var/lib/postgresql
